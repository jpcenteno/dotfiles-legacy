# Karabiner Config (Mac only)

This scripts ensures Karabiner is installed with my custom configs.

- Map caps lock to `ctrl` if held or `esc` if pressed and released.
- Maps `⌘ + ⇧ + ⮐ ` to open Terminal.app window.
